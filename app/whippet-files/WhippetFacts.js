const factOne = 'Whippets are a type of dog.'
const factTwo = 'Whippets are very fond of meat, so their favourite food is probably something like chicken or beef.'
const factThree = 'Whippets love to play fetch, and will often play for hours if their owner is willing to throw the ball for them.'

export function WhippetFacts() {
    const div = document.getElementById('brunch');
    console.log(div);
    // if(div) {
    //     div.innerHTML = `
    //         <h2 class="large-fact">${factOne}</h2>
    //         <p class="small-fact">${factTwo}</p>
    //         <h3 class="medium-fact">${factThree}</h3>
    //     `;
    // }
}